# 更新日志

本文件同时用于：

- 根目录版本更新记录维护
- GitHub Release 发布说明来源

书写规则：

- 每次发布新版本时，在顶部新增一个版本段
- 标题格式必须为 `## x.y.z - YYYY-MM-DD`
- 版本号必须与 Git tag 一致，例如 `1.0.1`
- GitHub Actions 会自动提取对应版本段内容作为 Release Notes

## 1.0.2 - 2026-05-17

### 新增

- WebDAV 自动同步新增“同步时间粒度”配置，支持按分、时、天设置
- 背景预览弹窗新增“显示模拟页面元素”开关，可模拟顶部书签栏与搜索框实际效果

### 优化

- 页面设置中的背景图片区域改为更紧凑的 `el-upload` 预览样式
- 背景预览弹窗调整为居中展示，并按当前窗口比例模拟真实显示效果
- 抽屉按钮布局继续收敛，移除多余的关闭按钮

### 修复

- 修复背景图片选择后保存设置但背景未即时更新的问题
- 修复书签栏与菜单项 `title` 提示未完整显示第二行链接地址的问题

## 1.0.1 - 2026-05-17

### 新增

- 右上角页面设置新增背景图片选择与清空能力
- 页面设置新增“点击书签后的全屏跳转遮罩”文字与颜色配置
- 搜索区书签结果点击现在会先展示“正在跳转...”占位层

### 重构

- 将搜索区从 `App.vue` 中拆分为 `features/search/components/SearchStage.vue`
- 新增 `searchPageStateService`、背景图服务与 shared storage 服务封装
- `App.vue` 调整为页面装配层，书签栏与搜索区职责边界更清晰

### 优化

- 搜索框内部不再承载背景图设置入口，统一收口到右上角页面设置
- WebDAV 配置抽屉与页面设置抽屉改为本地表单副本，避免直接修改 props
- 清理模板遗留示例 store 与无关注释

### 修复

- 修复顶部与下拉菜单点击书签时无法稳定展示跳转遮罩的问题
- 修复缓存变更后页面状态不同步的风险，补充 `chrome.storage.onChanged` 响应

## 1.0.0 - 2026-05-17

### 新增

- 支持通过 WebDAV 拉取并缓存 Chrome 导出书签
- 新标签页顶部增加接近 Chrome 书签栏的多级级联菜单
- 新标签页下方页面迁移为 Vue + Element Plus 组件化实现
- 支持搜索引擎切换、背景图设置、书签搜索结果浮层

### 优化

- 顶部书签栏支持窄宽度下的溢出书签折叠菜单
- 其他书签自动靠右显示
- 结果下拉、菜单 hover、active 状态等视觉细节统一为玻璃风格

### 修复

- 修复 WebDAV 书签解析在“智能助手”后结构丢失的问题
- 修复清空缓存后顶部书签栏需要刷新页面才消失的问题
- 修复刷新书签提示偶发显示为 `[object PointerEvent]` 的问题
